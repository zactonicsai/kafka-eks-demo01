# Auto-provision Prometheus as the default Grafana datasource on startup.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090   # in-network address of Prometheus
    isDefault: true
    editable: true
