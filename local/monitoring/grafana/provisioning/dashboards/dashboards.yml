# Tell Grafana to load any dashboard JSON from /var/lib/grafana/dashboards.
apiVersion: 1
providers:
  - name: "kafka-dashboards"
    orgId: 1
    folder: "Kafka"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
